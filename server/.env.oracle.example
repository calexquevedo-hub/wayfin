# Server Environment Variables (Oracle Always Free / Docker)

NODE_ENV=production
PORT=5000

# Internal Docker network MongoDB URI
MONGODB_URI=mongodb://mongodb:27017/wayfin

# IMPORTANT: generate a strong value (at least 32 chars)
JWT_SECRET=change-me-with-a-long-random-secret

# App public URL (used by clients and future integrations)
CORS_ORIGIN=http://YOUR_ORACLE_PUBLIC_IP
